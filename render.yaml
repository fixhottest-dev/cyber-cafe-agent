services:
  - type: web
    name: cyber-cafe-ai-agent
    env: python
    buildCommand: "pip install -r requirements.txt && playwright install chromium && playwright install-deps"
    startCommand: "uvicorn agent_server.py:app --host 0.0.0.0 --port $PORT"
